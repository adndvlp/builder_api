{
  "name": "osf-fire-relay",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "emulators": "firebase emulators:start --import=./emulator-backup --only functions,auth,firestore"
  },
  "dependencies": {
    "firebase-functions": "^6.6.0"
  }
}
